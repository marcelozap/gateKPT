# Public practice calendar

The homepage is public at the user's request. No login or environment secrets are required. No medical records are included. Completion history stays in each browser's localStorage. Search indexing remains disabled, which is not access protection.

Older routes remain archived behind the maintenance proxy. Deploy the existing Next.js project from main. Run `node test-access.mjs` with TEST_URL set to the deployment URL to verify public access.
